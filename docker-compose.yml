services:
  app:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - ./models/weights:/weights:ro
      - ./data/eval:/data/eval:ro
      - ./reports:/reports
    environment:
      STRIDE_MODEL_PATH: /weights/best.pt
    command: ["ui", "--host", "0.0.0.0", "--port", "7860"]
